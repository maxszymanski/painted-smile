---
import { Image } from 'astro:assets'
const { id, foto, is = false } = Astro.props
---

<Image
    src={foto}
    alt=`Zdjęcie z animacji nr${id}`
    class:list={['h-full w-full', { 'row-span-2': is }]}
/>
